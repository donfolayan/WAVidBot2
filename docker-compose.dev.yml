# Local dev override — builds the bot from source instead of pulling from Docker Hub.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up

services:
  wabotii:
    image: null
    build: .
